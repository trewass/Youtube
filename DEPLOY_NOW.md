# 🚀 Быстрый деплой на Vercel

## Вариант 1: Через Vercel CLI (быстро)

```bash
cd /Users/aleksandrkoza/Projects/Youtube-mp3
vercel --prod
```

При первом запуске:
1. Войди в аккаунт Vercel (откроется браузер)
2. Выбери scope (твой аккаунт)
3. Link to existing project? → N (создаем новый)
4. Project name → youtube-audiobooks (или любое другое)
5. Directory → ./frontend
6. Override settings? → Y
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Development Command: `npm run dev`

После деплоя получишь URL вида: `https://your-app.vercel.app`

---

## Вариант 2: Через GitHub (рекомендуется для автоматических деплоев)

1. **Закоммить и запушить код:**
   ```bash
   git add .
   git commit -m "Prepare for Vercel deployment"
   git push origin main
   ```

2. **На Vercel:**
   - Зайди на [vercel.com](https://vercel.com)
   - Add New Project
   - Выбери репозиторий Youtube-mp3
   - Настройки:
     - Framework Preset: Vite
     - Root Directory: `frontend`
     - Build Command: `npm run build`
     - Output Directory: `dist`
   - Deploy

3. **После деплоя:**
   - Vercel автоматически будет деплоить при каждом push в main
   - Получишь URL приложения

---

## ⚙️ Настройка переменных окружения (если нужно)

В Vercel Dashboard → Settings → Environment Variables:

```
VITE_API_URL=https://youtube-production-ee12.up.railway.app
```

(Но это не обязательно, так как в vercel.json уже настроены rewrites)

---

## ✅ Проверка после деплоя

1. Открой URL приложения
2. Открой консоль браузера (F12)
3. Проверь логи:
   - Должно быть: "🚀 App starting..."
   - Должно быть: "✅ App rendered successfully"
   - Должно быть: "API URL: ..."

4. Проверь что API работает:
   - Открой Network вкладку
   - Попробуй добавить канал
   - Проверь что запросы идут на правильный URL

---

## 🔧 Если что-то не работает

1. **Белый экран:**
   - Открой консоль браузера
   - Посмотри ошибки
   - Проверь что backend доступен: `https://youtube-production-ee12.up.railway.app/health`

2. **CORS ошибки:**
   - Убедись что backend разрешает запросы с твоего Vercel домена
   - Проверь настройки CORS в `backend/app/main.py`

3. **API не работает:**
   - Проверь что Railway backend запущен
   - Проверь логи в Railway Dashboard
   - Проверь что URL в vercel.json правильный

---

## 📝 Текущая конфигурация

- **Backend URL**: `https://youtube-production-ee12.up.railway.app`
- **Frontend**: Деплоится на Vercel
- **Rewrites**: `/api/*` и `/audio/*` проксируются на backend

Готово к деплою! 🎉
