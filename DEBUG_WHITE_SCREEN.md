# Диагностика белого экрана на Vercel

## Шаги для диагностики:

1. **Откройте консоль браузера (F12)**
   - Перейдите на вкладку Console
   - Посмотрите какие ошибки там есть
   - Должны быть логи: "🚀 App starting...", "✅ Rendering app...", "✅ App rendered successfully"

2. **Проверьте Network вкладку**
   - Откройте DevTools → Network
   - Перезагрузите страницу
   - Проверьте, загружаются ли файлы (index.html, JS, CSS)
   - Проверьте статус ответов (должны быть 200)

3. **Проверьте API URL**
   - В консоли должно быть: "API URL: ..."
   - Если там `http://localhost:8000` - это проблема!
   - Нужно установить переменную окружения `VITE_API_URL` в Vercel

## Решение:

### Если backend на отдельном домене:

1. В Vercel Dashboard → Settings → Environment Variables
2. Добавьте:
   ```
   VITE_API_URL=https://your-backend-url.com
   ```
3. Пересоберите проект

### Если backend на том же домене:

Убедитесь что переменная `VITE_API_URL` НЕ установлена (или пустая), тогда будет использоваться относительный путь.

## Частые ошибки:

- **CORS ошибка** - backend не разрешает запросы с фронтенда
- **404 на API** - неправильный API URL
- **Module not found** - проблема со сборкой
- **Syntax error** - ошибка в коде

## Что я исправил:

1. ✅ Добавил логирование для диагностики
2. ✅ Улучшил обработку ошибок
3. ✅ Исправил API URL для продакшена (использует относительный путь если нет переменной)
4. ✅ Добавил обработку ошибок рендеринга

Теперь откройте консоль браузера и посмотрите что там написано!
