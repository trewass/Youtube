# 🚀 Быстрый старт

## Требования

- **Python 3.11+**
- **Node.js 18+**
- **ffmpeg** (для конвертации аудио)

### Установка ffmpeg

**macOS:**
```bash
brew install ffmpeg
```

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install ffmpeg
```

**Windows:**
Скачайте с [ffmpeg.org](https://ffmpeg.org/download.html)

## Установка

### Автоматическая установка (рекомендуется)

```bash
./setup.sh
```

Этот скрипт:
- Проверит наличие всех зависимостей
- Создаст виртуальное окружение Python
- Установит все пакеты
- Создаст `.env` файлы

### Ручная установка

**Backend:**
```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
```

**Frontend:**
```bash
cd frontend
npm install
cp .env.example .env
```

## Конфигурация

Откройте `backend/.env` и настройте:

```env
# Обязательно для AI функций
OPENAI_API_KEY=your_openai_api_key_here

# База данных (SQLite по умолчанию, можно использовать PostgreSQL)
DATABASE_URL=sqlite:///./audiobooks.db

# Опционально - для приватных/возрастных видео
YOUTUBE_COOKIE_FILE=
```

### Получение OpenAI API ключа

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в раздел API keys
3. Создайте новый ключ
4. Скопируйте и вставьте в `.env`

## Запуск

### Автоматический запуск (оба сервера)

```bash
./start.sh
```

Приложение будет доступно по адресу:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000
- **API Документация:** http://localhost:8000/docs

### Ручной запуск

**Терминал 1 - Backend:**
```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --reload
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

## Первые шаги

1. Откройте http://localhost:3000
2. Перейдите в раздел "Каналы"
3. Добавьте ссылку на YouTube канал, например:
   - `https://www.youtube.com/@channel_name`
   - `https://www.youtube.com/channel/CHANNEL_ID`
4. Дождитесь парсинга плейлистов
5. Выберите плейлист и нажмите "Синхронизировать"
6. Выберите аудиокнигу и нажмите "Скачать"
7. Слушайте оффлайн и делайте заметки!

## Использование на телефоне (PWA)

1. Откройте приложение в браузере телефона
2. В Chrome: "Меню → Установить приложение"
3. В Safari: "Поделиться → На экран Домой"
4. Приложение будет работать как нативное с оффлайн доступом

## Возможные проблемы

### Backend не запускается

```bash
# Проверьте Python версию
python3 --version  # Должна быть 3.11+

# Переустановите зависимости
cd backend
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Frontend не запускается

```bash
# Очистите кеш и переустановите
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### Ошибка при скачивании видео

1. Убедитесь что ffmpeg установлен: `ffmpeg -version`
2. Проверьте что видео доступно (не приватное)
3. Для приватных видео экспортируйте cookies из браузера

### Ошибки AI

Убедитесь что:
- `OPENAI_API_KEY` настроен в `.env`
- У вас есть баланс на аккаунте OpenAI
- Интернет соединение работает

## Production Deploy

### Backend

```bash
cd backend
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

### Frontend

```bash
cd frontend
npm run build
# Разместите содержимое dist/ на любом статическом хостинге
```

### Docker (будет добавлено позже)

## Полезные команды

```bash
# Очистка базы данных
rm backend/audiobooks.db

# Очистка скачанных файлов
rm -rf storage/audio/*

# Обновление зависимостей
cd backend && pip install --upgrade -r requirements.txt
cd frontend && npm update

# Проверка логов
# Backend выводит логи в терминал
# Frontend логи в браузере (F12)
```

## Поддержка

Если возникли проблемы:
1. Проверьте все требования установлены
2. Посмотрите логи в терминале
3. Проверьте `.env` файлы
4. Откройте issue на GitHub

